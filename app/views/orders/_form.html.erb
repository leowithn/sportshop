<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :customer_id %><br>
    <%= f.select :customer_id, @customer_choice %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.select :product_id, @product_choice %>
  </div>
  <div class="field">
    <%= f.label :orderdate %><br>
    <%= f.date_select :orderdate %>
  </div>
  <div class="field">
    <%= f.label :ordernotes %><br>
    <%= f.text_area :ordernotes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
