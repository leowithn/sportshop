<script src="../_js/jquery-1.7.2.min.js"></script>
<script>
$(document).ready(function() {


 $('#submission').focus(function()
 {
$(this).css('background-color', 'tan')
 alert("Check all data for accuracy. Then click the submit button again.");
}); //end focus
}); //end ready
</script>


<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :categoryname %><br>
    <%= f.text_field :categoryname %>
  </div>
  <div class="field">
    <%= f.label :categorydescription %><br>
    <%= f.text_area :categorydescription %>
  </div>

    <p> <input name="submitButton" type="submit" id="submission" value="Submit Data" length = 20> </p>

<% end %>
